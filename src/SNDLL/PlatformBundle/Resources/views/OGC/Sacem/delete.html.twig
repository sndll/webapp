{% extends "SNDLLPlatformBundle:OGC:ogclayout.html.twig" %}

{% block title %}
    {{ parent() }} - Supprimer
{% endblock %}

{% block body %}
    {{ parent() }}
    <h2>Supprimer un centre Sacem</h2>

    <p>
        Etes-vous certain de vouloir supprimer le centre "{{ sacem.codesacem }}" ?
    </p>

    <form action="{{ path('sndll_ogc_sacem_delete', {'sacem_id': sacem.codesacem}) }}" method="post">
        <a href="{{ path('sndll_ogc_sacem_view', {'sacem_id': sacem.codesacem}) }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à la fiche
        </a>

        <input type="submit" value="Supprimer" class="btn btn-danger" />

        {{ form_rest(form) }}
    </form>

{% endblock %}